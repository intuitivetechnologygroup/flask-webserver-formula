# Default lookup dictionary

flask_webserver:
  www_user: nginx
  pip_dependencies:
    - virtualenv
    - uwsgi
  formula_dependencies:
    - nginx
  # formula_dependencies:
  #   - name: nginx-formula
  #     folder: nginx
